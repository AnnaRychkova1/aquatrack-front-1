// import { createAsyncThunk } from '@reduxjs/toolkit';
// import { requestGetDate } from './services';

// // GET @ /contacts
// export const apiGetDate = createAsyncThunk(
//   'phonebook/get',
//   async (_, thunkAPI) => {
//     try {
//       const data = await requestGetDate();
//       return data;
//     } catch (err) {
//       return thunkAPI.rejectWithValue(err.message);
//     }
//   }
// );
